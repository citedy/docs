---
title: Platform Overview
description: Citedy Agent Platform — architecture, authentication, and capabilities
---

# Citedy Agent Platform

**Version**: 1.7.0 | **Status**: Production

Citedy is a backend service for AI agents. Connect once, configure defaults in the web UI, and let any agent — Claude Code, Cursor, ChatGPT, or any MCP-compatible client — write posts, generate images, publish to social platforms, scan trends, and produce short-form video.

The web UI is for settings and scheduling review. The agent does the work.

## Authentication

All agent endpoints require:

```
Authorization: Bearer <AGENT_API_KEY>
```

Obtain your key at [citedy.com/dashboard/settings](https://www.citedy.com/dashboard/settings) or via `POST /api/agent/register`.

Key rotation: `POST /api/agent/rotate-key`

## Command Reference

| Agent Command | API Call | Cost |
|---|---|---|
| `/post <topic>` | POST /api/agent/post | 2-4 cr |
| `/write <topic>` | POST /api/agent/autopilot | 4-8 cr |
| `/shorts <prompt>` | POST /api/agent/shorts | 60-185 cr |
| `/shorts script` | POST /api/agent/shorts/script | 1 cr |
| `/shorts avatar` | POST /api/agent/shorts/avatar | 3 cr |
| `/shorts merge` | POST /api/agent/shorts/merge | 5 cr |
| `/publish now` | POST /api/agent/publish | 0 cr |
| `/schedule list` | GET /api/agent/schedule | 0 cr |
| `/schedule gaps` | GET /api/agent/schedule/gaps | 0 cr |
| `/scan fast` | POST /api/agent/scan (mode=fast) | 2 cr |
| `/scan deep` | POST /api/agent/scan (mode=deep) | 4 cr |
| `/scan ultra` | POST /api/agent/scan (mode=ultra) | 6 cr |
| `/scan ultra+` | POST /api/agent/scan (mode=ultra+) | 8 cr |
| `/products upload` | POST /api/agent/products | 1 cr |
| `/products list` | GET /api/agent/products | 0 cr |
| `/products search` | POST /api/agent/products/search | 0 cr |
| `/image-style` | PUT /api/agent/image-style | 0 cr |
| `/status` | GET /api/agent/status | 0 cr |
| `/settings` | GET /api/agent/settings | 0 cr |

## Platform Support Matrix

| Platform | Text | Images | Video |
|---|---|---|---|
| Twitter / X | Yes | Yes | Yes |
| LinkedIn | Yes | Yes | Yes |
| Facebook | Yes | Yes | Yes |
| Instagram | Yes | Yes | Reels |
| YouTube | - | - | Shorts |
| Reddit | Yes | Yes | No (link only) |
| Threads | Yes | Yes | - |
